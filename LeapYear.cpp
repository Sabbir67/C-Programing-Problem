#include <iostream>
using namespace std;

int main(){

    int year;
    cout<<"Please input a year ° ";
    cin>> year;

    if( (year % 4 == 0) && (year % 100 != 0)){
        cout << "This is a Leap Year ";
    }
        cout<<"This is not a L
    else if (year % 400 == 0 ){
        cout << "This is a Leap Year ";
    }
    else {eap Year";
    }

return 0 ;
}
